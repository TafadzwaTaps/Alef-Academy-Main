@model IEnumerable<Alef_Academy_Main.Models.AdministrativeUsers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdminUserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdminPassword)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdminEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdminUserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdminPassword)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdminEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdminUserId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdminUserId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdminUserId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
